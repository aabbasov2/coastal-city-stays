{
  "extends": "next/core-web-vitals",
  "rules": {
    // Add any custom rules here
  },
  "overrides": [
    {
      "files": ["**/*.ts", "**/*.tsx"],
      "rules": {
        // TypeScript specific rules can go here
      }
    }
  ]
}
